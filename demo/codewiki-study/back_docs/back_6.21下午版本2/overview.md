概览文档已生成至 `output_docs/overview.md`，包含以下内容：

**文档结构：**

| 章节 | 内容 |
|------|------|
| **§1 仓库目的** | 系统定位（家装销售合同全生命周期管理引擎）、4 项核心设计目标 |
| **§2 端到端架构** | 完整的 Mermaid 架构图，覆盖客户端层 → AOP 层 → 操作层 → 策略层 → 校验层 → PDF 层 → 基础设施 → 外部系统，共 8 层 |
| **§3 核心模块概览** | 7 个模块的路径、组件数、职责、关键设计模式一览表 |
| **§4 模块文档索引** | 7 个主模块 + 5 个子模块的文档链接和内容要点，可直接跳转 |
| **§5 模块间依赖关系** | Mermaid 依赖图，清晰展示横切层 → 核心操作 → 策略层 → PDF 层的单向依赖链 |
| **§6 合同生命周期** | 状态机图（Draft → 提交 → 签约 → 完成）+ 各阶段对应的模块和操作 |
| **§7 关键设计模式** | 10 种设计模式的应用位置和说明 |

**相比旧版的主要改进：**
- 架构图层次更清晰（从 5 层细化为 8 层），组件名称与实际源码一致
- 模块文档索引从 5 项扩充为 12 项（7 主模块 + 5 子模块）
- 新增合同生命周期状态机和阶段映射表
- 新增关键设计模式汇总表
- 依赖关系图标注了依赖类型（注解触发、委托调用、数据消费等）